{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tars.serviceAccountName" . }}
  labels:
    {{- include "tars.labels" . | nindent 4 }}
{{- end }}
